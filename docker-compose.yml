services:
  db:
    image: postgres:17-alpine
    container_name: lifeos-db
    environment:
      POSTGRES_USER: lifeos
      POSTGRES_PASSWORD: lifeos
      POSTGRES_DB: lifeos
    ports:
      - "5433:5432"
    volumes:
      - lifeos-pg:/var/lib/postgresql/data
volumes:
  lifeos-pg:
